# 喵流 (MeowFlow) - 产品需求文档

> 让每一份工作，像猫一样优雅流动
> Powered by 汉堡 🐱 (金渐层)

---

## 一、产品概述

### 1.1 产品定位

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                          │
│                          喵流 (MeowFlow)                                 │
│                                                                          │
│   一款面向业务人员的 AI 工作流自动化平台                                   │
│   无需编程，通过可视化拖拽 + 模板市场 + 智能向导                            │
│   快速构建企业级 AI 自动化流程                                            │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

**使命**：把 AI 能力从"工程师的玩具"变成"业务人员的工具"
**态度**：流程不顺？撸一撸就顺了 🐾

### 1.2 目标用户

| 用户层 | 占比 | 典型角色 | 核心诉求 |
|--------|------|----------|----------|
| 业务运营人员 | 70% | 电商运营、HR、客服、行政 | 自动化日常工作，零门槛使用AI |
| 团队主管 | 25% | HR主管、客服主管、行政主管 | 团队协作、效率提升、数据可见 |
| 企业决策者 | 5% | CTO、老板 | 快速验证、降本增效、私有部署 |

---

## 二、功能架构

### 2.1 核心功能模块

| 模块 | 功能 | 优先级 |
|------|------|--------|
| **M01 工作流管理** | CRUD、版本管理、导入导出、分组分类 | P0 |
| **M02 可视化编辑器** | 拖拽编排、节点配置、实时预览 | P0 |
| **M03 节点市场** | 34种节点（触发器/AI/流程/工具/通知） | P0 |
| **M04 模板市场** | 行业模板、一键使用、模板发布 | P0 |
| **M05 执行引擎** | DAG编排、节点调度、异步执行 | P0 |
| **M06 触发器** | Webhook、定时、表单、消息 | P0 |
| **M07 日志监控** | 执行日志、实时监控、告警通知 | P1 |
| **M08 用户权限** | 组织管理、角色权限、审计日志 | P1 |
| **M09 统计报表** | 执行统计、成本分析、数据导出 | P1 |
| **M10 集成中心** | 钉钉/企微/飞书、AI模型配置 | P1 |

### 2.2 节点类型

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          节点分类                                      │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  触发器节点 (Trigger)                                                    │
│  ├─ Webhook 触发器 - 接收外部 HTTP 请求                               │
│  ├─ 定时触发器 - Cron 表达式定时执行                                   │
│  ├─ 表单触发器 - 用户提交表单触发                                     │
│  └─ 消息触发器 - 接收 IM 消息触发                                     │
│                                                                          │
│  AI 节点 (AI)                                                          │
│  ├─ LLM 对话 - 调用大语言模型                                         │
│  ├─ AI 分类 - 文本分类                                                │
│  ├─ 信息提取 - 实体抽取/结构化                                        │
│  ├─ 文本总结 - 长文本摘要                                             │
│  └─ 知识库检索 - 多路检索（RAG）                                      │
│                                                                          │
│  流程控制节点 (Flow)                                                    │
│  ├─ 条件分支 - if/else 判断                                          │
│  ├─ 循环节点 - For/While 循环                                         │
│  ├─ 并行执行 - 多分支同时执行                                         │
│  └─ 等待节点 - 延时执行                                                │
│                                                                          │
│  工具节点 (Tool)                                                        │
│  ├─ HTTP 请求 - 发送 HTTP 请求                                        │
│  ├─ 数据库操作 - SQL 查询/写入                                        │
│  ├─ 代码执行 - JavaScript/Python 代码                                 │
│  └─ 变量赋值 - 设置/修改变量                                          │
│                                                                          │
│  通知节点 (Notify)                                                      │
│  ├─ 钉钉通知 - 发送钉钉群消息                                         │
│  ├─ 企业微信 - 发送企微消息                                            │
│  ├─ 飞书通知 - 发送飞书消息                                           │
│  ├─ 邮件发送 - SMTP 邮件                                              │
│  └─ 短信通知 - SMS 短信                                                │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 三、产品原型

### 3.1 页面清单

| 页面 | 功能 | 优先级 |
|------|------|--------|
| P01 工作流列表 | 工作流管理主界面 | P0 |
| P02 可视化编辑器 | 拖拽编排工作流 | P0 |
| P03 节点配置面板 | 节点参数配置 | P0 |
| P04 模板市场 | 模板浏览与使用 | P0 |
| P05 执行日志 | 执行记录与调试 | P1 |
| P06 统计看板 | 数据可视化仪表盘 | P1 |
| P07 用户中心 | 账号与权限管理 | P1 |
| P08 团队管理 | 组织架构管理 | P1 |
| P09 系统设置 | AI模型/集成配置 | P2 |

### 3.2 原型预览

原型文件：`D:\Code\喵流\docs\prototype\index.html`

```
┌──────────────────────────────────────────────────────────────────────┐
│  Logo    工作流 │ 模板 │ 统计 │ 设置        [帮助] [+新建] [头像]  │
├────────────┬─────────────────────────────────────┬───────────────────┤
│            │  统计栏：执行次数 │ 耗时 │ 成本     │                    │
│ 我的工作流 │─────────────────────────────────────│                    │
│            │  工具栏：撤销 │ 重做 │ 缩放 │ 运行  │                    │
│ [搜索...]  │─────────────────────────────────────│   配置面板        │
│            │                                      │                    │
│ ○ 智能客服│           画 布 区 域                │   [配置][输入][输出] │
│   运行中   │                                      │                    │
│            │      ┌───┐                          │   节点名称        │
│ ○ HR简历  │      │开始│                          │   AI 模型         │
│   草稿    │      └───┘                          │   Prompt          │
│            │        │                            │                    │
│ ○ 产品文案│        ▼                            │                    │
│   已停止   │      ┌───┐                          │                    │
│            │      │LLM │                          │                    │
│ ○ 会议纪要│      └───┘                          │                    │
│   运行中   │        │                            │                    │
│            │        ▼                            │                    │
│            │    ┌───────┐                        │                    │
│            │    │ 条件  │                        │                    │
│            │    └───┬───┘                        │                    │
│            │      是   否                         │                    │
│            │       │    │                        │                    │
│            │       ▼    ▼                        │                    │
│            │     ┌──┐ ┌──┐                       │                    │
│            │     │VIP│ │AI │                      │                    │
│            │     └──┘ └──┘                       │                    │
│            │                                      │                    │
├────────────┴─────────────────────────────────────┴───────────────────┤
│  执行日志                                        [清空] [▲]          │
│  14:30:01 [开始] 工作流开始执行...                                   │
│  14:30:02 [成功] AI 分类完成...                                     │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 四、技术方案

### 4.1 技术选型

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端 | Vue 3 + TypeScript + Element Plus | 主流技术，生态完善 |
| 可视化 | AntV G6 / LogicFlow | 流程图编辑能力 |
| 后端 | Java 17+ / Spring Boot 3.x | 稳定可靠，企业首选 |
| 数据库 | PostgreSQL 15+ | 支持 JSONB，适合工作流定义 |
| 缓存 | Redis 7+ | 会话、锁、限流 |
| 队列 | RabbitMQ 3.12 | 异步任务执行 |
| 对象存储 | MinIO | 文件、日志存储 |

### 4.2 三层架构设计（参考 Ragent）

> 架构设计参考了 **Ragent** 等优秀开源项目的工程实践

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    Ragent vs 喵流 架构对比                               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌────────────────┐  ┌────────────────────────────────────────────┐   │
│  │    Ragent      │  │              喵流                          │   │
│  ├────────────────┤  ├────────────────────────────────────────────┤   │
│  │                │  │                                            │   │
│  │ Bootstrap      │  │  业务服务层                                │   │
│  │ (业务层)       │  │  • WorkflowService                         │   │
│  │                │  │  • TemplateService                         │   │
│  │                │  │  • ExecutorService                         │   │
│  │                │  │                                            │   │
│  ├────────────────┤  ├────────────────────────────────────────────┤   │
│  │                │  │                                            │   │
│  │ Infra-AI       │  │  节点执行层                                │   │
│  │ (AI基础设施)  │  │  • NodeRegistry（节点注册中心）             │   │
│  │                │  │  • LLMExecutor + 模型路由 + 熔断降级      │   │
│  │                │  │  • SearchExecutor（多路检索）              │   │
│  │                │  │  • MCPToolExecutor（MCP 工具调用）        │   │
│  │                │  │                                            │   │
│  ├────────────────┤  ├────────────────────────────────────────────┤   │
│  │                │  │                                            │   │
│  │ Framework      │  │  基础设施层                                │   │
│  │ (通用框架)      │  │  • 8个专用线程池 + TTL透传              │   │
│  │                │  │  • 队列式限流 + 三态熔断器               │   │
│  │                │  │  • AOP链路追踪 + Trace ID                │   │
│  │                │  │                                            │   │
│  └────────────────┘  └────────────────────────────────────────────┘   │
│                                                                          │
│  核心思想：换模型不改业务，换节点不改引擎，换业务不改基础设施              │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### 4.3 节点执行层设计

#### 4.3.1 节点注册中心

```java
@Component
public class NodeRegistry {

    private final Map<String, NodeExecutor> executors = new ConcurrentHashMap<>();

    @PostConstruct
    public void init() {
        // LLM 节点
        register("llm", new LLMExecutor());
        register("llm-classify", new LLMClassifyExecutor());
        register("llm-extract", new LLMExtractExecutor());
        register("llm-summarize", new LLMSummarizeExecutor());

        // 流程控制节点
        register("condition", new ConditionExecutor());
        register("loop", new LoopExecutor());
        register("parallel", new ParallelExecutor());

        // 工具节点
        register("http", new HTTPExecutor());
        register("database", new DatabaseExecutor());
        register("code", new CodeExecutor());

        // 知识库检索节点（多路检索）
        register("knowledge-search", new KnowledgeSearchExecutor());

        // 通知节点
        register("dingtalk", new DingtalkNotifyExecutor());
        register("feishu", new FeishuNotifyExecutor());
        register("email", new EmailNotifyExecutor());
        register("sms", new SMSNotifyExecutor());
        register("wxwork", new WxWorkNotifyExecutor());
    }

    public NodeExecutor get(String type) {
        NodeExecutor executor = executors.get(type);
        if (executor == null) {
            throw new NodeNotFoundException(type);
        }
        return executor;
    }
}
```

**设计模式**：策略模式 + 注册表模式

#### 4.3.2 LLM 节点（参考 Ragent 模型路由）

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        模型路由与容错                                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │  核心特性：                                                       │    │
│  │  ├─ 多模型候选（按优先级排序）                                     │    │
│  │  ├─ 三态熔断器（CLOSED → OPEN → HALF_OPEN）                   │    │
│  │  ├─ 自动降级（首选失败自动切换备选）                             │    │
│  │  ├─ 首包探测（发现慢自动切换）                                   │    │
│  │  └─ 健康检查（定期探测可用性）                                    │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                                                          │
│  模型支持：                                                             │
│  ├─ OpenAI: GPT-4o, GPT-4o Mini                                    │
│  ├─ Anthropic: Claude 3.5 Sonnet                                     │
│  ├─ 阿里云: 通义千问 Plus/Max                                        │
│  └─ 百度: 文心一言 4.0                                                │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

```java
public class LLMNodeConfig {
    // 候选模型列表（按优先级排序）
    private List<ModelCandidate> candidates;

    // 熔断配置
    private CircuitBreakerConfig circuitBreaker;

    // 降级策略
    private FallbackStrategy fallback;
}
```

#### 4.3.3 知识库检索节点（参考 Ragent 多路检索）

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        多路检索架构                                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  用户问题                                                               │
│      │                                                                │
│      ▼                                                                │
│  ┌───────────────────────────────────────────────────────────────┐    │
│  │                   问题预处理                                    │    │
│  │  • 意图识别  • 关键词提取  • 同义词扩展                    │    │
│  └─────────────────────────┬────────────────────────────────────┘    │
│                            │                                          │
│          ┌─────────────────┼─────────────────┐                     │
│          ▼                 ▼                 ▼                      │
│    ┌───────────┐    ┌───────────┐    ┌───────────┐              │
│    │  向量检索 │    │ 关键词检索│    │ 知识图谱 │              │
│    │  (Vector)│    │  (BM25)  │    │  (Graph) │              │
│    └─────┬─────┘    └─────┬─────┘    └─────┬─────┘              │
│          └─────────────────┼─────────────────┘                     │
│                            ▼                                        │
│    ┌───────────────────────────────────────────────────────────────┐  │
│    │                   后处理链                                    │  │
│    │  • 去重  • 融合排序（RRF）  • 重排序  • 质量过滤       │  │
│    └─────────────────────────┬────────────────────────────────────┘  │
│                              ▼                                        │
│                         检索结果                                       │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

**设计模式**：策略模式 + 责任链模式

### 4.4 执行引擎设计

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          执行引擎流程                                  │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  触发请求                                                               │
│      │                                                                │
│      ▼                                                                │
│  加载工作流定义（JSON）→ 构建 DAG → 拓扑排序（Kahn算法）               │
│      │                                                                │
│      ▼                                                                │
│  ┌─────────────────────────────────────────────────────────────┐      │
│  │                   节点执行循环                               │      │
│  │                                                               │      │
│  │  for node in order:                                          │      │
│  │      │                                                       │      │
│  │      ├─ 获取执行器（NodeRegistry）                          │      │
│  │      ├─ 执行节点                                              │      │
│  │      ├─ 保存输出到上下文                                     │      │
│  │      └─ 错误处理（重试/熔断/回滚）                          │      │
│  │                                                               │      │
│  └─────────────────────────────────────────────────────────────┘      │
│      │                                                                │
│      ▼                                                                │
│  返回执行结果                                                           │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

```java
@Service
public class WorkflowEngine {

    @Autowired
    private NodeRegistry nodeRegistry;

    public ExecutionResult execute(String workflowId, Map<String, Object> input) {

        // 1. 加载工作流
        Workflow workflow = workflowRepository.findById(workflowId);

        // 2. 创建执行上下文（TTL透传用户上下文）
        ExecutionContext context = ExecutionContext.builder()
            .workflowId(workflowId)
            .input(input)
            .variables(new HashMap<>())
            .traceId(TraceContext.get())
            .userId(UserContextHolder.getUserId())
            .build();

        // 3. DAG 拓扑排序
        List<Node> executionOrder = dagSort(nodes, edges);

        // 4. 按序执行
        for (Node node : executionOrder) {
            NodeExecutor executor = nodeRegistry.get(node.getType());
            NodeResult result = executor.execute(context, node);
            context.setVariable(node.getId() + ".output", result.getOutput());
        }

        return ExecutionResult.success(context.getOutput());
    }
}
```

### 4.5 基础设施能力

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        基础设施能力                                      │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │  线程池管理（8个专用线程池）                                    │    │
│  ├─────────────────────────────────────────────────────────────────┤    │
│  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐             │    │
│  │  │ 工作流执行 │ │ LLM 调用   │ │ HTTP 请求  │             │    │
│  │  │ max-20    │ │ max-50    │ │ max-100  │             │    │
│  │  └─────────────┘ └─────────────┘ └─────────────┘             │    │
│  │  关键：所有线程池用 TtlExecutors 包装，确保上下文透传           │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │  限流熔断                                                        │    │
│  ├─────────────────────────────────────────────────────────────────┤    │
│  │  限流：队列式限流（Redis ZSET + Lua + Pub/Sub）               │    │
│  │  熔断：三态熔断器（CLOSED / OPEN / HALF_OPEN）                 │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │  链路追踪                                                        │    │
│  ├─────────────────────────────────────────────────────────────────┤    │
│  │  @TraceNode("LLM 调用")                                         │    │
│  │  自动记录：Trace ID / 节点ID / 时间 / 输入 / 输出 / 异常        │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### 4.6 项目结构

```
D:\Code\喵流\backend\meowflow\
│
├─ meowflow-common/                  # 通用模块（参考 Ragent Framework）
│   ├─ config/                      # 线程池、TTL配置
│   ├─ context/                    # 用户上下文、Trace上下文
│   ├─ exception/                  # 异常处理
│   ├─ rate/                       # 限流熔断
│   ├─ result/                     # 统一返回
│   ├─ trace/                      # 链路追踪
│   └─ util/                       # 工具类
│
├─ meowflow-infra/                  # AI基础设施层（参考 Ragent Infra-AI）
│   ├─ model/                      # ChatClient、EmbeddingClient抽象
│   ├─ router/                     # 模型路由、三态熔断器
│   ├─ search/                     # 多路检索、后处理器链
│   └─ mcp/                        # MCP工具
│
├─ meowflow-workflow/               # 工作流服务
│   ├─ engine/                     # 执行引擎核心
│   │   ├─ executor/               # 节点执行器（34种节点）
│   │   └─ result/                 # 执行结果
│   └─ repository/                 # 数据访问
│
├─ meowflow-template/               # 模板服务
├─ meowflow-user/                   # 用户服务
└─ meowflow-monitor/                # 监控服务
```

> ⚠️ **改名迁移任务**：Maven 包路径 `com.MeowFlow.*` → `com.meowflow.*`，目录 `MeowFlow-*` → `meowflow-*`
> 详细见 [技术架构文档](./technical/architecture.md)

---

## 五、开发计划

### 5.1 版本规划

| 版本 | 周期 | 功能范围 |
|------|------|----------|
| MVP v1.0 | 8-10周 | 核心功能：工作流/编辑器/节点/触发器/模板/监控 |
| v1.1 | 4周 | 企业功能：团队管理/权限/统计报表/集成增强 |
| v1.2 | 4周 | AI增强：AI生成流程/智能推荐/知识库检索 |
| v2.0 | 6周 | 开放平台：API/SDK/插件市场/私有部署 |

### 5.2 MVP功能清单

```
MVP 版本功能（8-10周）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

P0 核心功能：
├─ 用户模块：注册/登录/JWT认证
├─ 工作流：CRUD/版本管理/导入导出
├─ 可视化编辑器：拖拽/连线/配置
├─ 执行引擎：DAG拓扑排序/节点调度/错误处理
├─ 节点（20种）：
│   ├─ 触发器：Webhook、定时、手动
│   ├─ AI：LLM、分类、提取、总结
│   ├─ 流程：条件分支、循环、并行
│   ├─ 工具：HTTP、变量、代码
│   └─ 通知：钉钉、邮件
├─ 模板：5个预置模板
└─ 监控：执行日志/失败告警

参考 Ragent 的设计：
├─ 8个专用线程池
├─ 三态熔断器
├─ 链路追踪
└─ 统一响应/异常处理
```

---

## 六、竞争优势

### 6.1 竞品对比

| 维度 | 喵流 | Dify | Coze | Zapier |
|------|--------|------|------|--------|
| 目标用户 | 业务人员 | 开发者 | C端用户 | 企业 |
| 技术门槛 | 低 | 中 | 低 | 中 |
| 可视化 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| AI能力 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐ |
| 企业功能 | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐⭐ |
| 部署方式 | SaaS+私有 | 私有为主 | 仅SaaS | SaaS为主 |

### 6.2 差异化优势

1. **零门槛**：模板市场 + 向导模式 + AI生成
2. **企业级**：团队协作 + 权限管理 + 成本控制
3. **接地气**：按量计费 + 中文优化 + 国内模型支持

---

## 七、附录

### 7.1 参考项目

- **Ragent**: https://github.com/nageoffer/ragent - 企业级Agentic RAG智能体
- **Dify**: https://github.com/langgenius/dify - AI工作流开源项目
- **n8n**: https://github.com/n8n-io/n8n - 自动化工作流

### 7.2 技术文档

- [技术架构文档](./technical/architecture.md)
- [数据库设计](./technical/database.md)
- [HTML原型](./prototype/index.html)

---

**文档版本**: v3.0
**改名记录**: 2026-07-12 由"喵流 (MeowFlow)"更名为"喵流 (MeowFlow)"
**品牌化身**: 汉堡（金渐层猫）🐱
**最后更新**: 2026-07-12
