# 本地开发专用配置 - 仅在 spring.profiles.active=dev 时生效
# 注意事项:
# 1. 此处的 aes-key 是开发默认值,生产环境必须通过 Nacos / 环境变量 MEOWFLOW_CRYPTO_AES_KEY 覆盖
# 2. 跨服务保持同一 key,以便测试加解密
meowflow:
  crypto:
    aes-key: ${MEOWFLOW_CRYPTO_AES_KEY:m3dLu8JrJ22Jr0PJbM215/N/I4KOfjmNqzhHmJGav80=}
    key-id: dev-local